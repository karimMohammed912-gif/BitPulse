import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF009990); // Primary color (green)
  static const Color secondary = Color(0xFF074799); // Secondary color (green)
  static const Color backgroundLight =  Color.fromRGBO(232, 245, 233, 1); // Light mode background (white)
  static const Color backgroundDark = Color(0xFF000000); // Dark mode background (black)
  static const Color textLight = Color(0xFF000000); // Light mode text (black)
  static const Color textDark = Color(0xFFFFFFFF); // Dark mode text (white)
  //Colors.green.shade50
  // Additional colors for consistency
  static const Color success = Color(0xFF00CB6A);
  static const Color error = Color(0xFFFF4757);
  static const Color warning = Color(0xFFFFA726);
  static const Color grey = Color(0xFF5D5C5D);
  static const Color lightGrey = Color(0xFFF7F7F7);
}